<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thevarungupta.firebaseauthdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".realtimedatabase.ReadActivity"></activity>
        <activity android:name=".realtimedatabase.CreateActivity" />
        <activity android:name=".realtimedatabase.RealtimeDatabaseActivity" />
        <activity android:name=".phoneAuthentication.VerificationActivity" />
        <activity android:name=".phoneAuthentication.PhoneAuthActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".emailAuthentication.ProfileActivity" />
        <activity android:name=".emailAuthentication.ForgotPasswordActivity" />
        <activity android:name=".emailAuthentication.WelcomeActivity" />
        <activity android:name=".emailAuthentication.LoginActivity" />
        <activity android:name=".emailAuthentication.RegisterActivity" />
        <activity android:name=".emailAuthentication.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>